Definition:
	Loops are used to run code multiple times.

Types:
	for:
		used when the number of iterations is known
	while:
		used when the number of iterations is unknown
	do-while:
		similar to the while, except code executes at least once.

Syntax:
	for ( var declaration; condition; increment )

	while ( condition ) {

	}

	do {

	} while ( condition );
