"UI Config{{{
syntax enable				 "enable syntax
set number			    	 "line numbers
set showcmd				     "shows last command
set cursorline				 "shows which line the cursor is on
filetype indent on 			 "load file-specific indent files
set wildmenu				 "auto complete list for commands
set lazyredraw				 "selective redrawing
set showmatch				 "show matching brackets, etc
" }}}
"Search {{{
set incsearch				 "searches as you type
set hlsearch				 "highlights search
" }}}
"Folding {{{
set foldenable				 "enable folding
set foldlevelstart=10		 "opens most folds by default
set foldnestmax=10			 "10 nested fold max
set foldmethod=indent	 	 "fold based on indent level
"}}}
"Spaces and tabs {{{
set tabstop=4				 "number of visual spaces per tab
set softtabstop=4			 "number of space in tab when editing
set expandtab 				 "tabs are spaces
" }}}

" vim:foldmethod=marker:foldlevel=0
